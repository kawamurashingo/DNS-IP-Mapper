#!/bin/bash
# Default configuration for DNS-IP-Mapper

# IP Range Configuration
DEFAULT_IP_START=1
DEFAULT_IP_END=240

# Network Check Configuration
DEFAULT_TIMEOUT=2
DEFAULT_PARALLEL_JOBS=10

# Output Configuration
DEFAULT_OUTPUT_FORMAT="csv"
DEFAULT_VERBOSE=false

# Supported CIDR ranges
SUPPORTED_CIDRS=(20 21 22 23 24)

# Color Configuration
ENABLE_COLORS=true